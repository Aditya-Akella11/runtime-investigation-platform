<Project>
  <PropertyGroup>
    <!-- Suppress NU1900 (vulnerability data lookup) which can fail in CI environments without internet access -->
    <NoWarn>$(NoWarn);NU1900</NoWarn>
  </PropertyGroup>
</Project>
